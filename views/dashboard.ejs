<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Shipping Owl Dashboard</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background: #f6f6f7;
      padding: 2rem;
    }
    button {
      background: #5c6ac4;
      color: white;
      border: none;
      border-radius: 4px;
      margin: 0.5rem 0.5rem 0 0;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      cursor: pointer;
    }
    button:hover {
      background-color: #4b56a3;
    }
    #output {
      margin-top: 1.5rem;
      white-space: pre-wrap;
      border: 1px solid #ccc;
      padding: 1rem;
      background: white;
    }
  </style>
</head>
<body>
  <h1>🚀 Shipping Owl Dashboard</h1>
  <p>Local products in DB: <strong><%= productCount %></strong></p>

  <button id="seed">Seed Dummy Products</button>
  <button id="sync">Sync Products to Shopify</button>
  <button id="fetch">Fetch Orders from Shopify</button>

  <div id="output"></div>

  <script>
    async function callApi(path) {
      try {
        const res = await fetch(path);
        const text = await res.text();
        document.getElementById('output').innerText = text;
      } catch (err) {
        document.getElementById('output').innerText = 'Error: ' + err.message;
      }
    }

    document.getElementById('seed').addEventListener('click', () => callApi('/seed-products'));
    document.getElementById('sync').addEventListener('click', () => callApi('/sync-products'));
    document.getElementById('fetch').addEventListener('click', () => callApi('/fetch-orders'));
  </script>
</body>
</html>
