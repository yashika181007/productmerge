<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Shopify App Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 2rem; }
    button { margin: 0.5rem; padding: 0.5rem 1rem; font-size: 1rem; }
    #output { margin-top: 1rem; white-space: pre-wrap; border: 1px solid #ccc; padding: 1rem; }
  </style>
  <!-- ✅ Load App Bridge -->
  <script src="https://unpkg.com/@shopify/app-bridge@3"></script>
</head>
<body>
  <h1>App Dashboard</h1>
  <p>Local products in DB: <strong><%= productCount %></strong></p>

  <button id="seed">Seed Dummy Products</button>
  <button id="sync">Sync Products to Shopify</button>
  <button id="fetch">Fetch Orders from Shopify</button>

  <div id="output"></div>

  <script>
    const AppBridge = window['app-bridge'];
    const createApp = AppBridge.default;
    const { getSessionToken } = AppBridge.utils;

    const host = new URLSearchParams(window.location.search).get("host");
    const app = createApp({
      apiKey: "<%= process.env.SHOPIFY_API_KEY %>",
      host: host,
    });

    // 🔐 Secure API Call with Session Token
    async function callApi(path) {
      const token = await getSessionToken(app);
      const res = await fetch(path, {
        method: 'GET',
        headers: {
          'Authorization': `Bearer ${token}`
        }
      });
      const text = await res.text();
      document.getElementById('output').innerText = text;
    }

    document.getElementById('seed').onclick = () => callApi('/seed-products');
    document.getElementById('sync').onclick = () => callApi('/sync-products');
    document.getElementById('fetch').onclick = () => callApi('/fetch-orders');
  </script>
</body>
</html>
